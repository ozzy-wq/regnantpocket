import { GlassCard } from '@/components/ui';

export function StrategyCard() {
  return (
    <GlassCard>
      <p className="text-sm text-white/60">Copy-Trading</p>
      <h3 className="mt-1 text-xl font-semibold">Alpha Navigator</h3>
      <p className="mt-2 text-sm text-white/70">Demo strategist profile inspired by top quant setups. Mirror simulation only.</p>
    </GlassCard>
  );
}
